origin_resource = "endpoint"

provider_name = { "function!" = "{{- origin_resource.name | regexp(expr='s/^([^-]+)-.*/\\1/') | lower -}}" }
provider = { "json!" = "{{- provider_name -}}.json" }
services = { "json!" = "{{- provider_name -}}.json", jmespath = "values(roles)[?service == 'database' ] | [0]" }

[[create_resource]]
match_on = [{ property = "function", value = "database" }]
resource_type = "{{- origin_resource.function | lower -}}"
relation_type = "DEFINED_BY"
name = "{{- origin_resource.name | regexp(expr='s/^([^-]+-[^-]+-[^-]+)-.*/\\1/') | lower -}}"
[create_resource.properties]
function = "{{- params.solution -}}"
provider = "{{- provider_name -}}"
component = "{{- services.component -}}"
description = "Databases provided by {{ provider.display }} is an on-demand, scalable processing power—ranging from virtual machines to serverless containers—that is managed entirely through code. Access and inter-node communication rely on dynamic, short-lived IAM tokens and automated TLS certificates rather than static passwords."
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

origin_resource = "database"

decoder = {
    "rds" = "RDBMS",
    "rds-data" = "Serverless",
    "dynamodb" = "NoSQL",
    "elasticache" = "Memcached",
}

[[create_resource]]
create_from = { property = "component" }
relation_type = "DERIVED_FROM"
name = "{{- origin_resource.name | regexp(expr='s/^([^-]+-[^-]+)-.*/\\1/') | lower -}}-{{- decoder[property.value] | lower -}}-{{- origin_resource._type -}}"
[create_resource.properties]
function = "{{- property.value -}}"
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"
