origin_resource = "vault"

provider_name = { "function!" = "{{- origin_resource.name | regexp(expr='s/^([^-]+)-.*/\\1/') | lower -}}" }
provider = { "json!" = "{{- provider_name -}}.json" }

[[create_resource]]
create_from = { property = "wallet" }
relation_type = "DEPENDS_ON"
name = "{{ origin_resource.name | regexp(expr='s/^([^-]+)-.*/\\1/') | lower }}-{{- property.value | lower -}}-{{- provider.dictionary.wallet -}}"
[create_resource.properties]
function = "{{- property.value | lower -}}"
key = "{{- origin_resource.keys -}}"
secret = ["compute", "database", "iam", "vpn"]
certificate = ["compute", "storage", "database", "kubernetes", "loadbalancer", "dns", "iam", "vpn", "connectivity"]
created = "{{- now(utc=true) | date(format='%Y-%m-%dT%H:%M:%SZ') -}}"

origin_resource = "wallet"

[[link_resources]]
with = "subscription"
create_relation = { type = "SATISFIES" }
copy_properties = [
  { from = "portfolio", as = "token" }
]
